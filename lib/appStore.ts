// Set this to the live App Store URL on launch day. Until it's a non-empty
// string, the landing shows a "coming soon" state that points to the waitlist.
export const APP_STORE_URL = "";

export const APP_STORE_LIVE = APP_STORE_URL.length > 0;
