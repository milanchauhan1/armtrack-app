# Copy this file to .env.local and fill in the values from your Supabase project.
# Both vars are required for auth + data to work, and must be present at build time
# (they get baked into the static export / Capacitor bundle).

NEXT_PUBLIC_SUPABASE_URL=https://trjazxklaraausqtbwkg.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
